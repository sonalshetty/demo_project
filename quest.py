
#1. Define a function max() that takes two numbers as arguments and returns the largest of them.
#   Use the if-then-else construct available in Python.

def max(n1, n2):
    if n1 > n2:
        return n1
    else:
        return n2

in1 = eval(input("Enter the first values\n"))
in2 = eval(input("Enter the second values\n"))
maxnum= max(in1, in2)
print("The max value out of",in1,"&", in2,"is",maxnum)

#2. Define a function that computes the length of a given list or string.

def lngof(listname):
    for i in range(0:):
        l+=l
        print l

ln= eval(input("Enter string or list"))
ling = lngof("tree")
print(ling)
